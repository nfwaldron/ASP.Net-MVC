@* Create an HTML form with input fields for two numnbers and a button for adding the two numbers. Display the results of adding the
    two numbers on the form. Use the strongly typed helpers to create the form. Dont use the @HTML.LabelFor() helper to display the result.
    Instead use the Html.DisplayTextFor()
 *@

           
 